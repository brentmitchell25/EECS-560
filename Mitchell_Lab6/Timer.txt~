for n = 50000 : 400,000
    for 5 times
    	start timer
	      srand(i)
		for j = 1: 50,000(100,000 and so on) (or 1:n)
		    rand() % 4n
		    	  insert() BST
		end
	stop timer
	start timer
	      for 1:n/10
	      	  x = rand() / RAND_MAX
		  if 0 <= x < 0.25
		     deletemin
		  else if 0.25 <= < 0.5
		     deletemax
		  else if 0.5 <= x < 0.75
		       g = rand() % 4n
		       remove(g);
		  else
			g = rand() % 4n
			insert(g)
		  end if
	stop timer
	----------------------------
	start timer
	      srand(i)
	(Then same thing for 2-3 Tree) ...
