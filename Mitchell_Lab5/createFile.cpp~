#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <fstream>

#define table_size 600011
using namespace std;

int main(int argc, char** argv) {
  
  int load_factor = argv[1];
  int seed = argv[2];


  for(int i = 0.2 i <= 0.6; i+=.1) {
    for(int j = 0; j < 5; j++) {
      ofstream file;
      file.open("LoadFactor" + argv[1]);
      file << table_size << " ";
      for(int k = 0; i < table_size; k++) {
	long randomNumber = srand(seed) % (table_size * load_factor);
	file << randomNumber << " ";
      }
      file.close();
    }
  }

  return 0;
}
